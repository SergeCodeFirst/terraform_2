terraform {
    required_providers {
        aws = {
        source = "hashicorp/aws"
        version = "5.84.0"
        }
        # linode = {
        #     source = "linode/linode"
        #     version = "2.32.0"
        # }
    }
}